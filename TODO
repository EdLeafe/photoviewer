API
    - define a host website
    - register a viewer with the host website
    - load images from a website
    - move current images to an inactive folder
    - move inactive images to current folder
    - delete images from either folder
    - navigate among current images
